把 "{{ .TaskBranch }}" 合并到 "{{ .TargetBranch }}" 时发生了合并冲突。

任务标题：{{ .TaskTitle }}
任务描述：{{ .TaskDescription }}

请使用你的工具（read、edit、write_file）解决发生冲突的文件——只需编辑并测试更改即可解决冲突。
完成后，后端会自动提交并继续这次合并。

{{- if .ConflictedFiles }}
冲突的文件：
{{- range .ConflictedFiles }}
- {{ . }}
{{- end }}
{{- end }}

解决时，请分析每一侧更改背后的意图。当两侧的更改相互独立或彼此兼容时，把两侧结合起来。如果两侧表达的是真正冲突的意图（例如一侧新增某个功能而另一侧移除它），请先询问我该采用哪种做法，再继续。

解决完所有冲突文件后，运行所有相关的测试或检查（例如 build、test、lint），以确认解决是正确的。只有在你确信冲突已被妥善解决、并且项目仍能干净地构建和通过测试时，才调用 request_review。