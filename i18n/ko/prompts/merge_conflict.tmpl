"{{ .TaskBranch }}" 브랜치를 "{{ .TargetBranch }}" 브랜치에 병합하는 과정에서 충돌이 발생했습니다.

작업 제목: {{ .TaskTitle }}
작업 설명: {{ .TaskDescription }}

충돌한 파일은 도구 (read, edit, write_file)를 사용하여 해결하십시오 — 편집하고 테스트하여 충돌을 해결하면 됩니다.
작업을 마치면 백엔드가 자동으로 커밋하고 병합을 계속 진행합니다.

{{- if .ConflictedFiles }}
충돌한 파일:
{{- range .ConflictedFiles }}
- {{ . }}
{{- end }}
{{- end }}

해결할 때는 각 변경 뒤에 있는 의도를 분석하십시오. 두 쪽의 변경이 서로 독립적이거나 양립 가능하면 양쪽을 모두 반영하십시오. 두 쪽이 실제로 상충하는 의도를 표현하는 경우 (예를 들어 한쪽은 기능을 추가하고 다른 쪽은 그 기능을 제거하는 상황)에는, 작업을 진행하기 전에 어떤 방침을 취할지 저에게 물어보십시오.

충돌한 파일을 모두 해결한 후에는 관련 테스트나 검사 (예를 들어 build, test, lint)를 실행하여 해결 결과가 올바른지 확인하십시오. 충돌이 올바르게 해결되었고 프로젝트가 여전히 빌드와 테스트를 문제없이 통과한다고 확신할 수 있을 때에만 request_review를 호출하십시오.